---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="block text-xs text-white/90">
  {
    date.toLocaleDateString("lt-LT", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
